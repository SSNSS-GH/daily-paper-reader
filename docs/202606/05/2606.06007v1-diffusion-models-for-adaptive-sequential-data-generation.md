---
title: Diffusion Models for Adaptive Sequential Data Generation
title_zh: 面向自适应序列数据生成的扩散模型
authors: "Haoyang Cao, Minshuo Chen, Yinbin Han, Renyuan Xu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06007v1"
tags: ["query:symbolic-reg"]
score: 9.0
evidence: 提出自适应序列数据生成的扩散模型
tldr: 生成真实合成序列数据在金融、医疗等领域至关重要，但现有扩散模型直接扩展会丢失时序依赖且易泄露未来信息。本文提出序贯前向-反向扩散框架，通过逐步加噪去噪并基于历史条件确保自适应性，并引入新型得分匹配目标实现高效并行训练。理论上给出了得分近似、估计及分布估计的统计保证，实证在ARMA、高斯过程及均值-方差投资组合中验证了有效性。该工作为自适应序列生成提供了理论基础和实用方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06007v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1682, \"height\": 1033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06007v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1699, \"height\": 580, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06007v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06007v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1633, \"height\": 639, \"label\": \"Table\"}]"
motivation: 直接扩展扩散模型到序列数据会导致未来信息泄露，无法保持适应性和时序依赖。
method: 提出序贯前向-反向扩散框架，基于历史条件逐步加噪去噪，并设计新型得分匹配损失实现并行训练。
result: 理论上证明了得分近似、估计和分布估计的统计保证，实验验证在ARMA、高斯过程和投资组合中优于基线。
conclusion: 提出的自适应序列扩散模型生成高质量时序数据，在金融等场景中有效支持决策。
---

## 摘要
生成逼真的合成序列数据在运筹学、金融、医疗、能源系统和科学计算等现实应用中至关重要，这些领域利用时间索引观测进行预测、模拟、风险评估和数据驱动决策。虽然扩散模型在生成静态数据方面取得了显著成功，但其直接扩展到序列设置往往无法捕捉时间依赖性和信息结构。设计能够以自适应方式（即不预先知晓未来信息）模拟序列数据的扩散模型仍然是一个开放挑战。在这项工作中，我们提出了一个用于自适应时间序列生成的序列前向-后向扩散框架。我们的方法沿着序列逐步注入和去除噪声，以先前生成的历史为条件确保自适应性。引入了一种新的分数匹配目标以实现高效的并行训练。我们在一个通用框架下推导了严格的统计保证，然后建立了以ReLU网络作为具体实例的分数逼近、分数估计和分布估计结果。在实验上，我们在合成数据（包括ARMA模型和高斯过程）上验证了我们的方法，并展示了其在构建均值-方差最优投资组合中的有效性。

## Abstract
Generating realistic synthetic sequential data is critical in real-world applications across operations research, finance, healthcare, energy systems, and scientific computing, where time-indexed observations are used for prediction, simulation, risk assessment, and data-driven decision-making. While diffusion models have achieved remarkable success in generating static data, their direct extensions to sequential settings often fail to capture temporal dependence and information structure. Designing diffusion models that can simulate sequential data in an adapted manner, and hence without anticipation of future information, therefore remains an open challenge.   In this work, we propose a sequential forward-backward diffusion framework for adapted time series generation. Our approach progressively injects and removes noise along the sequence, conditioning on the previously generated history to ensure adaptiveness. A novel score-matching objective is introduced for efficient parallel training. We derive rigorous statistical guarantees under a generic framework, then establish score approximation, score estimation, and distribution estimation results with ReLU networks serving as a concrete instance. Empirically, we validate our method on synthetic data, including ARMA models and Gaussian processes, and demonstrate its effectiveness in constructing mean-variance optimal portfolios.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：面向自适应序列数据生成的扩散模型

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有扩散模型在生成静态数据（如图像）上表现优异，但直接将其扩展到序列数据生成时，会破坏时间信息流。反向扩散过程中的得分函数依赖于整个序列的联合分布，导致生成某个时间步的值时可能隐含地利用了未来信息，违反了序列数据固有的因果顺序（即“非预期性”或“自适应性”）。
- **研究动机**：在金融（如投资组合优化、压力测试）、医疗、能源等实际应用中，生成合成时序数据时必须尊重信息到达的时序——每个生成值只能基于当前及过去的信息，不能瞥见未来。现有方法（如Transformer自回归模型）在连续域上脆弱，依赖离散化引入误差；而现有的扩散模型未强制适应结构。
- **整体含义**：本文首次将扩散模型与自适应（adapted）时序生成相结合，提出了一个既能保持时序依赖又能保证信息流正确性的框架，并提供了完整的统计学习理论，为扩散模型在因果序列生成中的应用奠定了理论和算法基础。

## 2. 论文提出的方法论

### 核心思想
- 将完整时间序列的生成分解为一系列**条件序贯反向扩散过程**：每个时间步 \(h\) 独立运行一维的前向-反向SDE，但其反向过程的得分函数以之前已生成的完整历史 \(\bar{X}_{[1:h)}\) 为条件。
- 通过这种顺序生成，确保了每个新生成的坐标只依赖于过去，自然满足自适应（非预期）性质。

### 关键技术细节
- **前向过程**：对每个坐标 \(h\)，定义条件扩散：
  \[
  dX^h_t = -\frac12 X^h_t dt + dW^h_t,\quad X^h_0 \sim \phi^h_{\bar{X}^{[1:h)}_T}(0,\cdot)
  \]
  其中 \(\phi^h_z(t,\cdot)\) 是给定历史 \(z\) 下 \(X^h_t\) 的条件密度。
- **反向（采样）过程**：
  \[
  d\bar{X}^h_t = \left[\frac12 \bar{X}^h_t + \partial_x \log \phi^h_{\bar{X}^{[1:h)}_T}(T-t,\bar{X}^h_t)\right] dt + d\bar{W}^h_t
  \]
  使用标准高斯初始化，并通过学习到的得分网络 \(\hat{s}_h(t,x,z)\) 近似真实得分。
- **并行训练目标**：提出一个解耦的得分匹配损失（公式14），使得所有时间步的得分网络可以并行训练，无需逐序生成数据。损失使用去噪得分匹配形式，并引入了历史截断机制以减少输入维度。
- **采样算法（Algorithm 1）**：顺序执行每个时间步的反向过程到 \(T-t_0\)，每个步骤依赖之前生成的历史。

### 理论保证
- **适应性质**：Proposition 3.2 证明生成序列对特定的扩大滤子自适应。
- **统计学习理论**：在 Hölder 光滑性、次高斯性等假设下，给出了得分近似、得分估计和分布估计的有限样本界。关键创新是引入历史截断，利用时序依赖衰减将估计误差控制在与有效历史长度 \(d_{\text{trunc}}\) 相关的速率，而非全序列长度 \(H\)。具体地，总变差距离的边界为 \(\tilde{O}(n^{-\beta/(4(d_{\text{trunc}}+\beta)})\)，与静态扩散模型的最优率一致。
- 以ReLU网络为例，给出了具体的对数覆盖数界和收敛速率。

## 3. 实验设计

### 使用的数据集/场景
- **合成数据**：
  - ARMA(2,2) 模型：长度为 \(H=500\)，样本量 \(n\in\{2^8,2^9,2^{10},2^{11}\}\)。
  - 高斯过程（GP）：长度为 \(H=32\)，指数核 \(\gamma(i,j)=\exp(-|i-j|^\nu/\ell)\)，\(\ell=4,\nu=0.8\)。
- **真实数据**：S&P 500 日收益率数据（1990-2009训练，2010-2019测试），构建半年度投资组合（\(H=128\)个交易日）。

### Benchmark 与对比方法
- **ARMA实验**：对比扩散生成样本的ACF与训练数据直接计算的ACF相对于真实ACF的 \(\ell_1\) 相对误差（Diff RE1 vs Emp RE1）。
- **GP实验**：定性比较扩散生成样本的协方差矩阵与真实协方差矩阵。
- **投资组合优化**：比较四种数据来源下两种策略（Plug-in 和 RL）的性能：
  - 数据来源：Split（原始分割）、Split+Synthetic（使用[7]的SDE模拟路径）、Split+Ours（使用本文AD-Seq生成路径）、Bootstrap。
  - 策略：Plug-in（基于GBM估计参数）和 RL（连续时间Q学习[6]）。
  - 评估指标：平均收益、方差、Sharpe比率。

### 对比方法
- 直接使用训练数据（Empirical）
- 文献[7]的SDE模拟方法（Synthetic）
- Bootstrap重采样
- 无合成数据（仅Split）

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长等硬件/算力信息。仅在摘要末尾提供了代码仓库链接，但未给出具体计算资源细节。

## 5. 实验数量与充分性

- **实验数量**：
  - ARMA：4种样本量，每种重复5次，报告均值和标准差。
  - GP：仅一组实验（n=2048训练，2048生成），展示协方差可视化。
  - 投资组合：两个目标水平（z=1.10, 1.20），每种组合多次重复（括号内标准差）。
- **充分性评价**：
  - **优点**：ARMA实验覆盖了不同样本量，并量化了误差；投资组合对比了多种数据源和策略，验证了下游任务收益。
  - **不足**：
    - 未进行消融实验（如历史截断长度的影响、并行训练与顺序训练的对比）。
    - 未在高维（多变量）时间序列或更长序列上测试。
    - GP实验仅定性展示，缺乏定量指标（如协方差估计误差）。
    - 未与最新的时序扩散模型（如Diffusion-TS, TSGM等）进行直接比较。
  - **客观性与公平性**：对比方法选择合理（基线包括传统方法、Bootstrap和文献[7]），但缺少对模型复杂度的控制。

## 6. 论文的主要结论与发现

- AD-Seq 框架能生成保留时序依赖结构和自适应性质的高质量合成序列。
- 在ARMA模型中，扩散生成样本的ACF估计误差甚至小于直接使用训练数据，尤其在样本量小时优势明显。
- 在GP中，能够恢复核函数的指数衰减协方差模式。
- 在均值-方差投资组合优化中，使用AD-Seq生成的合成路径训练RL策略，获得了最高的Sharpe比率，优于其他数据源，包括文献[7]的SDE方法。
- 理论结果表明，在时序依赖衰减的条件下，估计误差率可与静态扩散模型的最优率相匹配，且序列长度不影响可学习性（仅依赖于有效历史长度）。

## 7. 优点

- **方法创新**：首次在扩散模型中显式保证自适应（非预期）性质，且通过条件并行训练实现了计算可扩展性。
- **理论完整**：提供了从得分近似到分布估计的端到端统计学习分析，特别是历史截断机制使理论适用于长序列和依赖衰减场景。
- **实验验证**：从合成数据到真实金融决策，证明了方法在统计保真度和下游决策质量上的双重优势。
- **代码开源**：提供GitHub仓库促进可复现性。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅测试了一维时间序列，未扩展到多维（多变量）序列。
  - 未在更复杂的长程依赖（如LSTM生成数据）或非平稳数据上验证。
  - 投资组合实验仅依赖S&P 500数据，泛化性存疑。
- **计算资源未报告**：缺乏关于训练成本（GPU、时间）的信息，不利于实际部署评估。
- **假设较强**：理论依赖于Hölder光滑性、次高斯性及依赖衰减假设，对于重尾或高度非平稳数据可能不成立。
- **未进行消融**：没有分析历史截断长度、早期停止时间 \(t_0\)、网络大小等超参数的影响。
- **仅对比有限基线**：未与最先进的时序扩散模型（如Diffusion-TS, CSDI等）直接比较。
- **潜在偏差**：由于使用Transformer因果掩码实现，但并未讨论其与框架的兼容性或理论等价性；投资组合策略的改进可能是由于合成数据增加了样本量而非模型优势，需要更多控制实验。

（完）
