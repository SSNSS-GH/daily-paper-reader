---
title: "Optimal Data Acquisition for Reinforcement Learning: A Large Deviations Perspective"
title_zh: 强化学习中的最优数据采集：大偏差视角
authors: "Mingjie Hu, Jian-Qiang Hu, Enlu Zhou"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28675v1"
tags: ["query:symbolic-reg"]
score: 9.0
evidence: 用于强化学习数据获取的大偏差框架
tldr: 针对强化学习中数据采集效率低下的问题，本文从大偏差理论出发，提出以策略选择错误概率的指数衰减率作为效率度量，并推导出嵌套优化问题的变分刻画。通过凸松弛和懒惰一步投影次梯度方法求解，设计了自适应数据采集策略。理论证明算法在常数因子内达到近似鲁棒最优，并扩展至线性函数近似以提升可扩展性。
source: arxiv
selection_source: fresh_fetch
motivation: 在成本高昂的交互场景（如医疗、运营）中，强化学习的数据采集效率亟需提升，现有方法缺乏统一的效率度量准则。
method: 基于马尔可夫链大偏差理论，推导策略选择错误概率的指数衰减率作为优化目标，构建嵌套优化问题并松弛为凸形式，使用懒惰一步投影次梯度法求解。
result: 提出自适应数据采集策略，证明其在给定最优性准则下实现常数因子内的近似鲁棒最优性，线性函数近似扩展有效。
conclusion: 为大偏差视角下的强化学习数据采集提供了统一框架，兼顾理论最优性与实际可求解性，在多个数值实验中验证效果。
---

## 摘要
数据采集效率是在商业和医疗运营中部署强化学习的一个核心挑战，在这些场景中，交互成本高昂、速度缓慢，并且通常涉及人在回路中。本文为无限时域强化学习中的数据采集开发了一个统一的大偏差框架。我们引入策略选择错误概率的指数衰减率作为原则性的效率指标，并通过马尔可夫链的大偏差理论推导出该速率的变分刻画，从而得到一个嵌套优化问题。基于这一刻画，我们形式化了两个互补的最优性概念，它们与嵌套问题的最优解相关。由于所得规划是隐式的且通常难以处理，我们提出了一种具有显式约束的可处理的凸松弛。随后，我们开发了一种懒惰的一步投影次梯度方法来求解松弛问题，并使用其迭代结果构建自适应数据采集策略。我们证明了所得的强化学习算法在我们的最优性准则下是近乎鲁棒最优的，误差在一个常数因子内。最后，我们将该框架扩展到线性函数逼近以提高可扩展性，数值实验支持了所提方法的有效性。

## Abstract
Data acquisition efficiency is a central challenge in deploying reinforcement learning in business and healthcare operations, where interactions are costly, slow, and often involve humans in the loop. This paper develops a unified large deviations framework for data acquisition in infinite-horizon reinforcement learning. We introduce the exponential decay rate of the policy-selection error probability as a principled efficiency metric and derive a variational characterization of this rate via large deviations theory for Markov chains, yielding a nested optimization problem. Based on this characterization, we formalize two complementary notions of optimality in terms of the optimal solution of the nested problem. Because the resulting program is implicit and generally intractable, we propose a tractable convex relaxation with explicit constraints. We then develop a lazy one-step projected subgradient method to solve the relaxed problem and use its iterates to construct an adaptive data acquisition policy. We prove that the resulting reinforcement learning algorithm is near-robustly optimal under our optimality criterion, up to a constant factor. Finally, we extend the framework to linear function approximation to improve scalability, and numerical experiments support the effectiveness of the proposed approach.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在商业和医疗运营等强化学习（RL）应用中，数据采集成本高昂、速度慢，且常涉及人在回路。现有RL算法大多针对固定置信度（δ-PAC）或最小化累积遗憾（regret）设计，而实际部署往往采用**固定预算**（fixed-budget）模式——给定总交互次数，目标是最大化正确选择最优策略的概率（PCS）。然而，固定预算场景缺乏统一的效率度量准则和理论指导。
- **整体含义**：本文尝试从**大偏差理论**（Large Deviations Theory）视角，为固定预算强化学习中的数据采集效率提供一个严格的数学框架。通过刻画策略选择错误概率的**指数衰减率**作为核心效率指标，将优化问题转化为变分优化，进而设计自适应采样算法，并给出最优性保证。这项工作填补了RL数据采集在固定预算下缺乏理论驱动的空白，为运营实验设计、医疗实验等场景提供了理论支撑。

## 2. 方法论

### 核心思想
- 提出**策略选择错误概率的指数衰减率** 作为效率度量：
  \[
  \lim_{T\to\infty} -\frac{1}{T}\log P(\hat{\pi}_T \neq \pi^*_M) = I
  \]
  其中 \(I\) 越大，表示错误概率下降越快，数据采集效率越高。
- 将全局错误事件分解为局部状态-动作对的错误事件，利用马尔可夫链的大偏差原理（Gärtner-Ellis定理和Perron-Frobenius定理）推导出各局部速率 \(G_{s,a}\) 的变分表示，进而得到全局速率为各局部速率的最小值。
- 基于此变分刻画，定义两种最优性：
  - **完全最优（Exact Optimality）**：要求行为策略收敛到嵌套优化问题的最优解，通常不可解。
  - **鲁棒最优（Robust Optimality）**：要求在困难MDP实例上达到渐近最优的指数衰减率，作为更务实的基准。
- 为解决原始嵌套优化问题的隐式性和非线性，进行以下关键松弛：
  1. 将行为策略下的稳态分布替换为**确定性分配向量 \(\omega\)**，并满足流平衡约束（\(W\)）。
  2. 对速率函数 \(I_1, I_2\) 使用二次下界（基于Bernstein型不等式），得到显式的下界。
  3. 对错误事件集 \(E_{s,a}\) 使用线性必要条件（Lemma 4）进行外逼近。
- 最终得到**凸优化问题**（Theorem 2）：
  \[
  \min_{\omega \in W} \max_{s,a \neq \pi^*(s)} L_{sa}(\omega, M)
  \]
  其中 \(L_{sa}\) 是平方最优性差距与方差、采样比例的加权形式。

### 关键技术细节
- **懒惰一步投影次梯度算法（LazyGradient，Algorithm 1）**：
  - 只在稀疏的更新时刻（\(t_n\)）进行一次投影次梯度下降，其他时间保持行为策略不变。
  - 使用Polyak平均得到平均迭代 \(\omega_n\) 作为目标分配的代理。
  - 行为策略混合了均匀策略（\(\epsilon_t\)-greedy）以保证持续探索。
  - 最终输出基于最终经验MDP的策略。
- **理论保证**：
  - Lemma 8：所有状态-动作对被无限次访问（几乎必然）。
  - Theorem 3：平均迭代收敛到最优目标值 \(F^*\)。
  - Theorem 4：算法达到**近鲁棒最优**，与最优速率相差因子 \(1-\gamma\)。
- **扩展到线性MDP**：利用线性函数假设（特征映射 \(\phi\)），推导类似的凸优化问题，并给出两个可计算的代理目标，但最优性保证未完全建立。

## 3. 实验设计

### 数据集/场景
1. **Gridworld**：\(S=16\) 状态，\(A=4\) 动作，随机转移，折扣因子 \(\gamma=0.99\)。奖励带噪声，方差 0.006–0.02。
2. **产品发布实验案例（Operational Experiment Design）**：\(S=50\) 状态（市场粗粒度上下文），\(A=30\) 动作（折扣与曝光组合），奖励和转移由行为学机制生成，方差各异。折扣因子 \(\gamma=0.99\)。

### Benchmark 方法
- Q-learning（模型无关，值函数）
- Actor-Critic（策略梯度baseline）
- PPO / TRPO（近端策略/信任区域策略优化）
- PSRL（后验采样，模型驱动）
- QOCBA（基于不确定性量化的模型驱动方法，仅在Gridworld中运行，因规模过大在案例中无法运行）

### 对比指标
- **概率正确选择（PCS）**
- **状态平均策略价值（Policy Value）**

### 额外实验
- Gridworld中比较了不同初始化策略（最大覆盖 vs. 均匀）和不同初始化预算 \(n_0\)（150–550）的效果。
- 案例研究中比较了不同状态大小（40–70）的影响。

## 4. 资源与算力

- 文中**未明确说明**所使用的GPU型号、数量或训练时长。
- 所有实验在标准台式机或服务器上运行，Gridworld进行了50次独立宏重复，案例进行了5次独立宏重复。推断算力需求较低，不依赖大规模并行计算。

## 5. 实验数量与充分性

- **实验数量**：Gridworld在多个预算（T=600,800,1000,1200）下各进行50次重复，案例仅T=2000下5次重复。额外消融实验（初始化策略、初始化预算、状态规模）各50次重复。
- **充分性**：
  - 对比方法覆盖主流模型无关、策略梯度、贝叶斯、启发式模型驱动方法，**全面且公平**。
  - 重复次数在Gridworld中足够，案例中因状态-动作对较多（1500个）导致QOCBA无法运行，但LazyGradient仍与其他方法对比，结论可信。
  - 消融实验验证了方法对初始化不敏感，且在不同规模下一致优。
- **潜在不足**：案例重复仅5次，置信区间可能较宽；未在不同困难程度实例上测试；未与最新大偏差方法（如Shi et al. 2025）直接对比（但该文是启发式，本工作理论更完备）。

## 6. 主要结论与发现

- **核心结论**：LazyGradient在所有预算下显著优于所有baseline，特别是在低预算时正确选择概率和策略价值更高，且收敛到近鲁棒最优。
- **具体结果**：
  - Gridworld中，T=600时PCS达0.78，而最佳baseline QOCBA仅为0.46；T=800时已达0.92。
  - 案例中，T=2000时LazyGradient策略价值27.37，次优方法PSRL仅23.46。
  - 初始化策略和初始化预算对性能影响微弱，方法鲁棒。
- **理论验证**：证明了算法几乎必然访问所有状态-动作对，且目标函数收敛至最优，并在困难实例上实现近鲁棒最优（因子 \((1-\gamma)\)）。

## 7. 优点

- **理论创新**：首次将大偏差理论系统应用于RL数据采集，提出指数衰减率作为效率度量，并给出完整的变分刻画和最优性定义。
- **算法高效**：懒惰更新仅需一次子梯度步，计算开销极小，适合在线自适应场景。
- **可扩展性**：通过线性MDP扩展至大规模问题，保持凸优化结构。
- **实验验证**：在多个场景下展示了一致且显著的性能提升，消融实验表明鲁棒性。

## 8. 不足与局限

- **实验局限**：
  - 案例研究仅5次重复，可能不够稳定；状态空间最大50，动作30，尚属中等规模，未在大规模真实环境中测试。
  - 未与最近基于大偏差的启发式方法（如Shi et al. 2025）直接对比。
  - 未探讨超参数（如更新间隔增长速率 \(\tilde{c}\)、步长 \(\eta_n\)、\(\epsilon_t\)衰减）的敏感性。
- **理论局限**：
  - 线性MDP扩展仅给出下界和代理问题，**未证明最优性**。
  - 近似鲁棒最优的常数因子 \((1-\gamma)\) 在近折扣因子时较大，实际可能不紧。
  - 假设最优策略唯一、MDP遍历、行为策略有全支持，部分假设在实际中可能不满足。
- **应用限制**：
  - 仅适用于固定预算纯探索场景，不适合在线学习或固定置信度场景。
  - 要求已知状态空间和动作空间有限且较小，否则计算成本高。

（完）
